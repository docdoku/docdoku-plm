---
layout: default
title: "'Allo, 'Allo!"
---

        <div class="container">
            <div class="jumbotron">
                <p>
		{{ site.lang }}

		and

		{{ site.baseurl_root }}</br></p>

<p>
		{% if site.lang == "fr" %}
    Liens 
  {% else if site.lang == "en" %}
    {% capture link2 %}{{ site.baseurl_root }}{{ page.title  }}{% endcapture %}
    <a href="{{ link2 }}" >{% t global.english %}</a>
  {% endif %}

		</p>
            </div>
        </div>


{% for post in site.posts %}

    {{ post.date | date: "%B %e, %Y" }}
    <a href="{{ post.url }}">{{ post.title }}</a>

{% endfor %}


