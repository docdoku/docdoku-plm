var APP_GLOBAL = {
    // Application Router
    ROUTER:{},
    // Application signals
    SIGNALS:{},
    // Sync between local storage and memory
    GLOBAL_CONF:{}
};
var OS_SLASH = os.type() == "Windows_NT" ? "\\" : "/";
