/*global define*/
define(['backbone'], function (Backbone) {
    'use strict';
    var ChatMessage = Backbone.Model.extend({

        initialize: function () {
        }

    });

    return ChatMessage;

});
